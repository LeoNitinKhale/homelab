# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Django
/staticfiles/
/media/
*.log

# Environment / secrets — never commit real values
.env
*.env
!.env.example
ansible/secrets.yml
ansible/*.secret

# Database dumps / backups (per CLAUDE.md — never commit these)
/backups/
*.sql
*.sql.gz
*.dump
*.pg_dump
*.sqlite3

# Personal source-data exports kept out of the repo
original_data/*.xlsx
original_data/*.xlsm
original_data/*.csv

# Raw mnemonics source imports (docx/xlsx/pdf/images) — bulky personal binaries,
# kept out of the repo; the curated figures.yaml + *.md are the source of truth.
data/mnemonics/original_data/
Thumbs.db

# YouTrack seed state — local map of figure id -> issue id (per-instance, not source)
data/mnemonics/.youtrack_map.json

# IDE
.vscode/
.idea/

# Claude Code local settings
.claude/settings.local.json
