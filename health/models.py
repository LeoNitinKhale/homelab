"""Health dashboard — TBD (core DB).

App is declared so the structure exists and migrations are wired, but the data
model is not yet specced. Add models here once the health dashboard is designed.
"""

# from common.models import TimestampedModel
# from django.db import models
